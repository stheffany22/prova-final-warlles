#include <stdio.h>
#include <math.h>

int main() {
    float a, b, h;

    printf("Digite os valores dos catetos a e b: ");
    scanf("%f %f", &a, &b);

    h = sqrt((a * a) + (b * b));

    printf("Hipotenusa = %.2f\n", h);

    return 0;
}
